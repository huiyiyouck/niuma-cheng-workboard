{
  "name": "niuma-cheng-workboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "项目管理工作台 — 牛马程生态只读聚合看板（本地 Node 服务 + frontend 静态产物）",
  "scripts": {
    "start": "node src/server/index.js",
    "dev": "node src/server/index.js --dev",
    "build": "npm --prefix frontend run build",
    "test": "node --test"
  }
}
