# v0.3 PRD

> 定性：**打磨型 patch 迭代**。补全 v0.2 上线后的对话查看体验、会话匹配准确度、菜单信息架构与若干看板/部署打磨；**本迭代不引入对被监控项目 / 业务数据的增删改查**（会话映射、手动打标签等 workboard **本地配置 / 元数据**写不属此列——不回写被监控项目）。
> 配套依据：BCR-013 评估游标 `agent-workflow/docs/progress/ad-hoc/2026-07-12-bcr-013-session-role-iteration-marker.md`（会话↔角色↔迭代匹配「转 workboard 解析侧治理」的方案与基线数据）。

## 文档状态
- 整体状态：待Review
- Review 轮次：R1
- 关联迭代：v0.3

## Review 计划
- Review 模式：动态 Review
- 影响领域：
  - 对话查看器状态模型提升（从「只吃 sessionId」到「项目上下文 + 角色映射」）、迭代维读取各项目仓 `.git` 历史的数据源可行性 → **Architect**
  - 对话查看器整体前端重构（Tab 切角色 / 页内内联配置 / 视觉打磨）、`detectRole` 后端改进、迭代维 INDEX-git-history 重建的工程成本 → **Developer**
- 本轮指定 Review 方：
  - **Architect**：① 对话查看器承载「项目 + 角色映射」上下文的数据流与状态模型；② 迭代维 INDEX-git-history 重建（会话 cwd + 时间戳 → 项目 `INDEX.md` git 历史「当前迭代」）的数据源可行性，尤其**本地开发 + 生产环境读各项目仓 `.git` 的可行性**；③ `detectRole` 扩展输入源（扫已入库 assistant 消息）的架构影响；④ **手动打标签 `manualRole` 数据模型**（本地存储 + 归类逻辑 `manualRole‖detectedRole‖General`）；⑤ 沟通详情读 coordination `communications/` 文档的数据源可行性（US-8）；⑥ **Codex 会话数据源同步** + `source` 列 schema 变更 + 多来源会话进同一匹配闭环的可行性（US-12）
  - **Developer**：① 对话查看器重构（角色 Tab、页内换/配会话、视觉打磨）的前端工程成本；② `detectRole` 改进 + 兜底 `Unknown→General` 的后端改动；③ 迭代维 git 历史解析的工程成本与性能；④ 菜单 5→3 信息架构重构的前端成本；⑤ **拖拽打标签交互** + `manualRole` 本地持久化；⑥ 沟通文档读取 + Markdown 渲染（US-8）；⑦ 看板待办折叠 / 长文本（US-7）；⑧ 生产错误页静态资源（US-9）；⑨ **Codex 解析器**（`codex-parser.js` 现状）+ schema `source` 列 + 1:N 迁移的工程成本（US-11/12）
- 未指定角色及原因：
  - UI：界面要点已并入本 PRD（BCR-004，PM 覆盖），不单独出 UI 方案；实现还原度由 PM 顺带核
  - DevOps：不参与 PRD 评审；但 **US-9 生产错误页**（nginx `error_page` + 静态页）由 DevOps 在**部署就绪检查阶段**落地
- 少于 2 个 Review 方时的用户确认：不适用（已指定 2 方）
- 定稿条件：本轮指定 Review 方全部通过，且无未处理高严重度阻塞问题

## Review 状态
| Review 方 | 指定原因 | 状态 | 轮次 |
|-----------|----------|------|------|
| Architect | 对话查看器状态模型 / 迭代维读 .git / detectRole 输入源 / manualRole 数据模型 / 沟通文档读取 | 待Review | R1 |
| Developer | 前端重构 + 拖拽交互 + 后端解析/文档读取 + 菜单重构 + 错误页 + 本地存储工程成本 | 待Review | R1 |

## Review 记录

## 1. 迭代目标

打磨「项目管理工作台」v0.2 上线后暴露的多处体验（**不对被监控项目 / 业务数据可写**）：

1. **对话查看器从「单调播放器」升级为「项目内工作台」**：进入某会话的对话后，可在页内横向切换同项目其他角色、直接换/配会话，不必返回上一级；同时改善对话本身的视觉层级与可读性。
2. **会话匹配准确度落到「解析侧正解」**：按 BCR-013 评估结论（不进框架、转 workboard 解析侧），把角色识别与迭代归属做实——角色维改进 `detectRole` 并兜底 `General`，迭代维新增 INDEX-git-history 重建 session↔迭代。
3. **菜单信息架构精简 5→3**（方案 A，独立可砍）：顶级菜单收敛为「看板 / 项目会话 / 需求池」。

另含搭车项：会话匹配的**强限制 + 拖拽纠错闭环**（US-3/10）、**1:N 多会话切换**（US-11）、**多来源会话同步 Codex**（US-12）、需求池**沟通详情**（US-8）、看板**待办折叠 / 长文本**（US-7）、生产**友好错误页**（US-9）。

**本迭代不做**：对被监控项目 / 业务数据的增删改查、对话输入交互、任何 agent-workflow 框架侧改动。（会话映射、手动打标签等 workboard 本地配置 / 元数据写不属此列——不回写被监控项目。）

## 2. 用户故事

### A 组 · 对话查看器三件套（主线）

- **US-1 对话视觉打磨**：作为 Owner，我在查看某会话对话时，想看到层级清晰、可读性好的界面，而不是单调的两色气泡堆叠，以便快速读懂这段对话在讲什么。
- **US-2 页内横向切角色**：作为 Owner，我进入某项目某角色的对话后，想在**同一页面内**直接切换到该项目的其他角色（如从产品切到开发、架构师），以便对照不同角色的工作，而不用返回角色卡片页再重新进入。
- **US-3 页内配会话（1:N）**：作为 Owner，我在对话查看器内为某角色**配置其归属的会话**——一个角色可归属**多个**会话（通过识别 / 打标签归入）；当角色下还没有会话时，页内给我选择入口把会话归入。
- **US-11 同角色多会话切换（当前 vs 历史）**：作为 Owner，当某角色下有多个会话时，我希望它们区分为**一个「当前会话」+ 若干「历史会话」**——当前默认是时间最新的那个、可手动标记（含把某历史会话设为当前），每角色**有且仅一个**当前；我能在当前与历史会话间切换查看（像回溯对话历史），以便对照该角色不同时期的工作。

### B 组 · 匹配增强（解析侧治理，方案依 BCR-013）

- **US-4 角色识别改进 + 兜底 General**：作为 Owner，我希望会话的角色标签尽量识别准确，且**识别不出角色的会话统一归为 General**（而非悬空的 Unknown），以便会话列表没有"无角色"的孤儿项。
- **US-5 迭代归属重建 + 迭代标签**：作为 Owner，我希望每个会话能标出它**大致属于哪个迭代**（按会话活动时间 × 项目 `INDEX.md` git 历史重建），并在会话选择器里显示「所属迭代」标签，以便我在页内配会话时快速找到「v0.2 的 PM 会话」这类目标。
- **US-10 拖拽打标签纠错**：作为 Owner，当某会话角色被自动识别错时，我希望通过**拖拽**把它从当前角色拖到正确角色（拖到目标角色 Tab / 放置区）来改其角色标签（覆盖自动识别），直观地把会话归到正确角色，形成「自动识别 → General 兜底 → 拖拽纠错」完整闭环。
- **US-12 多来源会话同步（Codex）**：作为 Owner，我希望 workboard 除 Claude Code 会话外，也同步 **Codex** 的会话，让 Codex 会话一并进入匹配闭环（识别 / 打标签 / 归类 / 切换）并带**来源标签**区分，以便统管多来源 AI 会话。（Trae CN 存储加密无解，标记为已知边界、暂不支持。）

### C 组 · 菜单精简（方案 A，独立可砍）

- **US-6 顶级菜单 5→3**：作为 Owner，我希望顶级菜单从「工作台 / 部署 / 接入诊断 / 跨项目 / 项目会话」收敛为「看板 / 项目会话 / 需求池」，部署与接入诊断降级进看板项目卡片详情抽屉的 tab，以便菜单更聚焦、减少并列冗余。
- **US-8 需求池·沟通可查看内容**：作为 Owner，我希望在需求池的「沟通」tab 里点击某条沟通，能看到该沟通的完整内容，而不只是一句摘要，以便追溯跨项目沟通的来龙去脉。

### D 组 · 看板打磨

- **US-7 跨项目待办折叠**：作为 Owner，我希望看板的「跨项目待办」在条数较多时默认折叠（只显示前几条），鼠标 hover 能预览更多、点击可展开全部，以便看板不被长列表撑开、保持整洁美观。

### E 组 · 部署健壮性

- **US-9 生产友好错误页**：作为 Owner，我希望生产环境在后端不可用（500/502）或访问无效路径（404）时，显示与 workboard 风格一致的友好错误页，而非裸 nginx 报错页，以便（尤其参赛演示时）保持专业观感。

## 3. 验收标准

### US-1 对话视觉打磨
- 当打开任一会话对话时，应该以区分度更高的视觉呈现 user / assistant / 工具调用消息（如角色化配色、头像或标识、消息间留白与分隔），而非仅靠两种底色色块区分。
- 当消息正文含结构化内容（代码块 / 列表 / 标题等 Markdown）时，应该以可读的格式呈现，而非整段纯文本平铺（最低要求：代码块等宽块状呈现）。
- 当对话内容为空或全为工具调用时，应该显示明确的占位提示，而非空白页。

### US-2 页内横向切角色
- 当我在对话查看器内点击另一个角色的 Tab 时，应该在**当前页面**加载该角色所映射会话的对话，页面不返回上一级。
- 当我切换到的角色**已映射会话**时，应该正确加载该会话内容；切换应有明确的当前选中态标识。
- 当某项目角色数量变化或某角色被禁用时，Tab 区应正确反映当前可切换的角色集合，不出现指向已失效映射的死 Tab。

### US-3 页内配会话（1:N）
- 当为某角色配置会话时，选择器应**只列识别为该角色的会话**（强限制）；General 列识别不出的会话；一个角色可归属**多个**会话。
- 当某角色下**还没有归属会话**时，应显示空态 + 内联会话选择入口，而非报错或空白；选中即把会话归入该角色。
- 当会话数据源不可用（如本地开发数据库未连）时，选择入口应显示明确的加载失败提示，而非静默无响应。
- 会话通过识别（`detectRole`）或手动打标签（US-10）归入角色；同一会话归属唯一角色（打标签 = 从原角色移出、归入新角色）。

### US-11 同角色多会话切换（当前会话 vs 历史会话）
- 每个角色的会话应区分为**一个当前会话 + 若干历史会话**；当前会话**默认 = 该角色时间最新的会话**，其余为历史。
- 当我把某会话（**含历史会话**）**手动标记为当前**时，该角色当前会话应更新为它、原当前降为历史；**手动标记后锁定**——即使之后出现更新的会话，当前仍为手动标记的那个，直到我再次修改或恢复默认（跟随最新）。
- 每个角色**有且仅有一个**当前会话。
- 当前会话应在界面上**明确区别于历史会话**（如突出 / 置顶）；历史会话可回溯查看；切换查看不改变会话的角色归属，也不改变「当前会话」的标记。
- 当某角色只有一个会话时，它即当前会话、无历史（切换 UI 可退化直显）；无归属会话时显示空态选择入口（同 US-3）。

### US-4 角色识别改进 + 兜底 General
- 当某会话经 `detectRole` 无法判定具体工作流角色时，应该归类为 `General`，会话列表中不再出现 `Unknown` 标签。
- 当会话首条用户消息为「你是 {角色}」等明确信号时，应该正确识别为对应角色（不因兜底逻辑回归而误判为 General）。
- 在真实开发项目会话样本上，改进后开发角色会话的「识别不出具体角色」比例应不高于改进前（以基线 ~7.5% 为参照，不劣化）。

### US-5 迭代归属重建 + 迭代标签
- 当某会话可按「会话活动时间 × 项目 `INDEX.md` git 历史」定位到某迭代时，应该在会话选择器/会话展示中显示对应「所属迭代」标签（如 `v0.2`）。
- 当某会话**无法定位到具体迭代**（如活动时段该项目「当前迭代=无」、会话早于 INDEX、非工作流项目）时，应该显示为「未归属 / 未识别」之类的中性标注，而非错误地硬套一个迭代。
- 迭代标签应标明其为**推断结果**（如来源提示「按活动时间推断」），不呈现为绝对确定值。

### US-10 拖拽打标签纠错
- 当拖动某会话到另一个角色（目标角色 Tab / 放置区）并释放时，应把该会话角色标签改为目标角色（**覆盖**自动识别），会话随即归入目标角色分类（如从 General 拖到产品，则出现在产品 Tab 的会话选择器里）。
- 拖拽过程应有明确视觉反馈：拖起态、可放置目标高亮、释放确认。
- 手动标签应**持久化**（workboard 本地存储），刷新后保留；**不回写**被监控项目 / 会话源文件。
- 触屏或不便拖拽时，应有备选入口（如长按 / 右键菜单选角色）兜底改角色。

### US-12 多来源会话同步（Codex）
- 当本机有 Codex 会话时，应被同步入库、出现在会话池，与 Claude 会话一起参与角色归类 / 打标签 / 切换（US-3/10/11）。
- 每个会话应显示**来源标签**（Claude / Codex），可区分来源。
- Trae CN 因存储加密无法读取正文，应标记为「暂不支持」的已知边界，不报错、不误导。

### US-6 顶级菜单 5→3
- 当进入应用时，顶级菜单应显示为「看板 / 项目会话 / 需求池」三项。
- 原「部署」「接入诊断」的信息应可在看板项目卡片详情抽屉的对应 tab 内访问到，不丢失能力。
- 原「跨项目」的需求池 / BCR / 谁等谁 / 沟通等内容应在「需求池」入口下可访问（作为子 tab 或分区），不丢失能力。

### US-7 跨项目待办折叠
- 当跨项目待办不超过阈值（默认 5 条）时，应全部直接显示，不出现折叠控件。
- 当超过阈值时，应默认只显示前 5 条 + 底部「展开全部（还有 X 条）」入口。
- 当 hover 折叠入口时，应给出更多待办的预览/提示，且不破坏页面布局与其余区块位置。
- 当点击展开时，应内联展开全部（超高时可滚动），入口变「收起」；再点收起恢复折叠态。
- 当**单条待办文本过长**（一行显示不全）时，应默认截断（最多两行 + 省略号）；hover 显示完整内容（悬浮浮层，不挤动表格布局），或点击展开查看全部。此项与「列表条数折叠」为两个独立维度。

### US-8 需求池·沟通查看完整内容
- 当在需求池「沟通」tab 点击某条沟通时，应打开抽屉展示该沟通对应 `communications/{REQ-id}-*.md` 文档的**完整内容**（Markdown 渲染），而非仅一句摘要。
- 当对应沟通文档缺失或读取失败时，应显示明确提示（如「沟通文档未找到」），而非空白或裸错误。
- 文档只读展示、不可编辑（符合 workboard 只读定位）。

### US-9 生产友好错误页
- 当后端服务不可用（如 502 / 500）时，应返回与 workboard 视觉一致的友好错误页（如「服务暂时不可用」），而非 nginx 默认裸报错页。
- 当访问不存在的资源（404）时，应显示友好的 404 页，或由 SPA 兜底进 app 的「页面不存在」提示，不出现裸报错。
- 错误页须能在生产 nginx 配置中正确挂载（`error_page` 指向静态页），且视觉与 workboard 一致。

## 3A. 界面要点（v0.3 设计定稿）

> 设计溯源：v0.3 界面要点经 claude.ai/design 共创后**文字化落定于本节**（v0.2 shadcn/ui 设计系统 1:1 收录 + v0.3 组件共创）。整体复用 v0.2 视觉：浅色中后台、主色 `#030213`、10px 圆角、字重 ≤500、系统字体。**实现以 `frontend/` 真代码 + 本节界面要点为准**（design 项目仅为共创过程产物，可不留存）。

### 界面布局示意（ASCII 线框 · Review 用）

**① 项目会话 · 两级导航**
```
项目会话
├ 第一级 · 项目选择：   [小报]  [ai]  [workboard]   ← 点项目卡
│                            │ 选中项目
└ 第二级 · 对话查看器  ◄──────┘    （顶栏「返回」回到第一级）
```

**② 对话查看器（A 组三件套 + B 组标签 + 1:N，核心）**
```
┌ 顶栏 ───────────────────────────────────────────────
│  ←  会话标题                        🏷 所属迭代 v0.2·推断    (US-5)
├ 角色 Tab（把会话拖到某 Tab = 改该会话角色归类 US-10）─────
│  ● 产品    架构    开发    运维    General              (US-2)
├ 会话切换器（同角色多会话，1:N）────────────────────────
│  ● 会话1    会话2    会话3   …                          (US-11)
├ 对话区（角色化气泡 + Markdown / 代码块）─────────────────
│              [USER] 启动 v0.3 迭代…            14:02     (US-1)
│   14:02  [AI 牛] 好的…  ┌ 代码块 ┐
├ 底部 ───────────────────────────────────────────────
│  只读模式 · v0.4+ 支持输入
```

**③ 未映射空态 + 强限制选择器 + 打标签纠错**
```
角色 Tab：  产品   架构   开发   运维   ● General
─ 空态 ──────────────────────────────────────────────
  「General」还没有映射会话
   从识别为该角色的会话中选（识别错 → 拖到别的角色 Tab 纠错）
─ 会话选择器（只列识别为 General 的会话 = 强限制 US-3）──────
   🔍 搜索标题 / 项目…
   • 会话A   42条·2天前    [General ⌄]  [v0.2]      ← ⌄/右键菜单改角色
   • 会话B   17条·5天前    [General ⌄]  [未归属]     ← 或拖到角色 Tab (US-10)
```

**④ 菜单 5→3（AppSidebar）**
```
┌ AppSidebar ──────
│ ▣ 项目管理工作台
│    统一管理中枢
│ ─────────────
│ ■ 看板            ← 深色实心选中态
│   项目会话
│   需求池
│ ─────────────
│ 🧭 参赛导览  ↗     ← 外链 /tour.html
└──────────────────
  部署 + 接入诊断 → 看板项目卡片详情抽屉 tab ；跨项目 → 需求池（谁等谁/沟通/BCR 子 tab）
```

**⑤ 看板 · 跨项目待办折叠（US-7）**
```
┌ 跨项目待办 ────────────────────  聚合自 INDEX · 只读
│ 优先级 │ 待办（≤2 行截断，hover 浮层看全文）│ 项目 │ 角色 │ 状态
│  P0    │ …                              │ …   │ …   │ …
│  P1    │ …             （默认前 5 条）      │
├──────────────────────────────────────────────────
│           展开全部 · 还有 3 条  ⌄          ← hover 浮现 peek 预览
└  （>5 折叠；点击展开可滚动；单条长文本 hover 悬浮浮层看全）
```

**⑥ 需求池 · 沟通点击看全文（US-8）**
```
需求池 tab：  需求池   谁等谁   ● 沟通   BCR
─ 沟通列表 ──────────────         点击某条  →   ─ 抽屉 ─────────────
  • REQ-1042   news↔l1   查看全文→               REQ-1042 · 沟通记录
    摘要（2 行截断）…                             ┌ Markdown 全文 ┐
                                                 └───────────────┘
                                                 只读 · 聚合自 communications
```

### 整体导航（C 组）
- 顶级菜单 3 项：**看板 / 项目会话 / 需求池**（`AppSidebar`）。
- **部署 + 接入诊断**降级进看板项目卡片详情抽屉的 tab（`ProjectDetailDrawer`）；**跨项目**收敛为**需求池**（`CrossProjectView`：需求池/谁等谁/沟通/BCR 四子 tab）。
- 需求池「沟通」子 tab：条目**可点击 → 抽屉展示对应 `communications` 文档全文**（Markdown 渲染、只读，US-8）。
- 看板「跨项目待办」折叠（US-7）：默认前 5 条 + hover 预览 + 点击展开/收起（详见「看板打磨」）。

### 项目会话 · 两级导航（A 组外层）
1. **第一级·项目选择**：进「项目会话」默认展示业务/工作板子项目卡片网格，点项目进第二级。
2. **第二级·对话查看器**：加载所选项目的对话；顶栏返回按钮回到第一级项目选择（非回看板）。

### 对话查看器（A 组三件套 + B 组标签）
- **顶栏**：返回 + 会话标题 + 项目名·消息数 + **迭代标签**「所属迭代 X · 推断」（US-5，标注推断、非绝对值）。
- **角色 Tab 排**（`RoleTabs`，US-2）：该项目 5 角色（产品/架构/开发/运维/General），每个 Tab 显示映射会话标题 + 是否已映射；点 Tab 页内横切、不返回。
- **对话区**（US-1）：`ChatMessage` 气泡 + 角色化头像/配色（`RoleAvatar`）+ Markdown/代码块渲染（`Markdown`）。
- **未映射角色空态**（US-3）：`RoleAvatar` + 「还没映射会话」+ 内联 `SessionPicker`（候选会话列表，每条带角色标签 + 迭代标签 + **来源标签**（Claude / Codex，US-12），选中即映射并加载）。
- **强限制 + 拖拽纠错**（US-3/US-10）：会话选择器**只列该角色会话**（General 兜底未识别）；会话条目可**拖拽到目标角色 Tab** 改标签（拖起态 / 目标高亮 / 释放确认的视觉反馈，备选长按或菜单兜底）。
- **当前会话 vs 历史会话**（US-3/US-11，1:N）：一角色可归属**多个**会话，区分为**一个当前会话（突出 / 置顶）+ 历史会话（可回溯列表）**；当前默认最新、可手动「设为当前」（含历史会话）、每角色唯一；历史会话上提供「设为当前」入口。单会话时直显，无会话时空态选择入口。
- **角色维兜底**（US-4）：Tab 含 General，识别不出角色的会话归 General（无 Unknown 孤儿）。
- 底部：只读提示（v0.4+ 支持输入）。

### 看板打磨（D 组）
- 跨项目待办（`CrossTodoList`）：默认显示前 5 条；超过则折叠，底部「展开全部（还有 X 条）」入口；hover 预览更多、点击内联展开/收起；折叠↔展开过渡平滑，不挤动看板其余区块，保持整洁。
- 跨项目待办**单条文本过长**：「待办」列默认截断（≤2 行 + 省略号），hover 悬浮浮层显示完整文本（不挤布局）/ 或点击展开；与「列表折叠」互不影响。

### 交互状态
- 加载中 / 空（未映射→空态选择器、无消息→占位）/ 错误（数据源不可用提示）/ 成功各态覆盖。

## 4. 范围边界

**本迭代做**：
- A 组：对话查看器视觉打磨、页内角色 Tab 横向切换、页内配会话（**1:N，一角色可多会话**，US-3）+ **同角色多会话切换**（US-11）+ 未映射空态内联选择器。
- B 组：`detectRole` 改进 + 兜底 `Unknown→General`；迭代维 INDEX-git-history 重建 session↔迭代 + 会话选择器「所属迭代」标签；**角色 Tab 强限制**（只列该角色会话）+ **手动打标签纠错**（会话角色可手动改、workboard 本地持久化，US-10）+ **多来源会话同步**（Codex 入池 + 来源标签，US-12；Trae CN 挂起）。
- C 组：顶级菜单 5→3（方案 A），部署 + 接入诊断降级进看板项目卡片详情抽屉 tab。
- D 组：看板「跨项目待办」折叠（默认前 5 条 + hover 预览 + 点击展开/收起）。
- C 组补充：需求池「沟通」条目可点击查看完整内容（US-8）。
- E 组：生产 404 / 500 / 502 友好错误页（US-9）。

**本迭代不做**：
- 任何增删改查 / 可写管理能力（Owner 明确本次不引入）。
- 对话输入 / 对话交互（留 v0.4+）。
- 任何 agent-workflow 框架侧改动（BCR-013 已评估关闭·不进框架；框架方案 C + hook 仅留档备选，不在本迭代）。

## 5. 前置依赖

- **BCR-013 评估结论**（已关闭·不进框架，转 workboard 解析侧治理）：本迭代 B 组的方案与基线数据依据。游标见 agent-workflow `docs/progress/ad-hoc/2026-07-12-bcr-013-session-role-iteration-marker.md`。
- **各项目仓 `.git` 历史可读**：迭代维 INDEX-git-history 重建要求 workboard 后端能读各被监控项目仓的 `.git`（本地开发 + 生产环境均需）。参谋长基线摸底在服务器侧已验证可读，但**本条须由 Architect / Developer 在设计 / 实现阶段正式确认**（尤其生产部署形态下的仓路径与读取权限）。
- **现有会话数据管道**：v0.2 已落地的 `claude-sync.js`（user + assistant 均已入库）、`session-meta.js`（`detectRole`）、角色↔会话映射 API 与 `SessionSelect` 组件。B 组角色维改进「只改 `detectRole` 输入源，数据管道不动」（BCR-013 已核实）。
- **coordination `communications/` 文档可读**（US-8）：沟通详情要求 workboard 后端读 coordination 仓 `communications/{REQ-id}-*.md` 全文（现只读 `REQUESTS`/`STATUS`/`PROJECTS`）——新增文档读取 + Markdown 渲染，Developer 实现。
- **生产 nginx `error_page` 配置**（US-9）：友好错误页需 DevOps 在部署阶段配置 nginx `error_page` 指向静态页——归部署就绪检查 / DevOps。
- **手动标签本地存储**（US-10）：会话新增 `manualRole` 字段存 workboard 本地 DB（角色归类 = `manualRole || detectedRole || General`），**不回写**会话源 / 被监控项目；Developer 定存储方案（可复用现有映射配置持久化机制）。
- **1:N 会话归属模型 + 当前会话**（US-3/US-11）：角色 → 会话**列表**（不再是 v0.2 的单个映射）；每会话按 `manualRole‖detectedRole‖General` 归入唯一角色；每角色的**当前会话** = `manualCurrent[角色] ‖ 该角色时间最新会话`（新增 `manualCurrent` 本地字段、可空、手动标记后锁定），有且仅一个。此外维护「每角色当前查看的会话」（切换查看用，与"当前会话标记"是两回事）。**v0.2 的 1:1 映射需迁移到 1:N**，由 Architect / Developer 在设计阶段定数据结构与迁移。
- **Codex 会话同步**（US-12）：schema 加 `source` 列区分来源（Claude / Codex）；`src/server/sync/codex-parser.js` 已存在（状态待 Developer 确认，可能 stub）；依据 Spike `ad-hoc/2026-07-05-spike-multi-source-session-sync.md`（Codex 可行、Trae CN 加密挂起）。多来源会话共用同一匹配闭环（识别/打标签/切换）。

## 6. 风险和开放问题

- **静态快照假设的固有局限**（BCR-013 点破，供实现与验收知情）：「一会话 = 一角色一迭代」并不总成立——一个会话可能先关 v0.2 再启 v0.3、参谋长/General 会话不属任何迭代、迭代可能在会话中途推进。因此**迭代标签是「尽力而为的归属」而非绝对真相**，UI 须留余地（标来源、允许中性「未归属」态）。此为已知局限，不追求全覆盖。
- **迭代维命中率天花板**：基线摸底开发角色会话命中 62.8%，未命中多为结构性不可归属（当时无迭代/老会话）。开放问题：未命中会话的展示口径（统一「未归属」还是细分原因），设计阶段定。
- **生产环境 `.git` 可读性**：生产部署下各项目仓是否以可读 `.git` 形态存在，需 DevOps / Architect 在部署就绪检查前确认；若不可读，迭代维需降级方案（开放项）。
- **`detectRole` 扫 assistant 消息的收益**：作为角色维可选增强（进一步降 Unknown），其成本/收益由 Developer 在设计/实现阶段评估，非必做；兜底 `General` 为必做项。
- **菜单 5→3 量级**：C 组是本迭代唯一的信息架构重构，量级最大、与 A/B 主题不同。已约定**独立成组、验收独立、实现最后做**，必要时可单独砍出而不影响 A/B 主线。
- **hover 浮层被 `overflow-hidden` 裁剪**（US-7 实现注意）：`CrossTodoList` 外层 `overflow-hidden`（为圆角裁切）会裁掉向下弹出的 hover 浮层——待办全文浮层与折叠 peek 预览都是 `absolute top-full`，末几行/底部按钮的浮层会溢出容器被裁。design 稿用少量示例数据看不出；实现时 Developer 须处理（浮层 portal 到 body、或改圆角实现方式），确保浮层完整可见。
- **沟通文档路径映射**（US-8）：`commItems` 的 `reqId` → `communications/{REQ-id}-短名.md`，文件名含「短名」需匹配（按 `REQ-id` 前缀扫目录定位），设计阶段定映射规则；找不到时按验收走「未找到」提示。
- **`coordination.test.js` 假失败**（跨任务待办已登记）：与本迭代无直接关系，但若实现阶段触碰会话/解析测试，可顺带评估是否 fixture 化（归 Developer 判断，不强绑本迭代范围）。

## 7. 实现指引（design 蓝本 → frontend 复现路径）

> 给实现阶段 Developer：如何用 design 原型图 + 本 PRD 落到 `frontend/` 真代码。**核心：翻译不是复制；后端从零。**

### design 原型图的定位
claude.ai/design 项目 `niuma-cheng-workboard` 的组件是**高保真视觉 + 交互蓝本**，**不是可直接部署的生产代码**——它跑在 design 沙盒（`window.React`、design 自建 `lib/*` 与 primitives、`sampleData` 演示数据），与 `frontend/`（Vite + 标准 import + `lucide-react` + shadcn/ui + 真实 `/api/snapshot`）**四层不兼容，照搬跑不起来**。**原型图留存、别弃**——它是实现阶段的视觉蓝本。

### 前端：翻译，不是复制（中等工作量）
Developer 用 **DesignSync 读出 design 组件完整代码**当蓝本，翻译成 frontend 生产组件：
- `const React = window.React` → 标准 `import`；
- design `lib/icons.jsx` / `cn.js` / 自建 primitives → `lucide-react` / shadcn/ui `@/components/ui/*`；
- `sampleData` → 真实 snapshot 数据；
- **保留 Tailwind `className` + 交互逻辑**——frontend 同用 Tailwind + 同套 token，视觉与交互即高度还原。

### 后端：从零做（工作量大头，design 无后端）
design 是纯前端 mock，**所有数据管道要从零实现**（详见 §5 前置依赖）：
- US-11 `manualCurrent` / `manualRole` 本地持久化 + v0.2 的 1:1→1:N 迁移；
- US-12 Codex 解析器（`codex-parser.js` 现状核实）+ schema `source` 列；
- US-8 `communications/*.md` 读取 + Markdown 渲染；
- US-5 INDEX-git-history 重建；US-4 `detectRole` 改进 + 兜底 General。

### 工作量判断
**复现 ≈ 前端翻译（中等）+ 后端从零（大头）。** 原型图完整的是「长什么样」，不是「数据怎么来」——真正成本在后端，Developer 评估工程成本时以此为准。

## Review 记录
