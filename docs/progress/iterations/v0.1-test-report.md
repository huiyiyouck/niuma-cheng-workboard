# v0.1 测试报告

## 文档状态
- 整体状态：已定稿
- Review 轮次：R1
- 关联迭代：v0.1

## 结论
- 最终判定：通过
- 质量判断：v0.1 只读看板满足 PRD AC-1.1~AC-8.2 的本地验收要求；未发现阻塞缺陷或高严重度缺陷。
- 下一步：交由 DevOps 执行生产部署前检查与生产部署。

## 执行范围

- 后端自动化：配置解析、路径解析、HTTP API、Markdown 表格解析、项目 INDEX 解析、项目匹配、角色日志解析、workflow-source 摘要、coordination 解析、整批快照。
- 前端构建：`npm run build`。
- 本地运行：`PORT=5174 HOST=127.0.0.1 npm start`。
- API 验收：`GET /api/snapshot`。
- 页面冒烟：工作台、部署、接入诊断、跨项目四视图。

## 结果摘要

| 类型 | 数量 |
|------|------|
| 通过 | 14 |
| 失败 | 0 |
| 阻塞 | 0 |

## 执行证据

| 项目 | 命令 / 方法 | 结果 |
|------|-------------|------|
| 后端自动化 | `npm test` | 45/45 通过 |
| 前端构建 | `npm run build` | Vite build 通过，生成 `frontend/dist` |
| 本地 API | `GET http://127.0.0.1:5174/api/snapshot` | HTTP 200；projects=4，diagnostics=5，todos=4，abnormal=0 |
| 页面冒烟 | Playwright Chromium | 工作台 / 部署 / 接入诊断 / 跨项目均可打开，控制台错误 0 |

## 缺陷列表

| 编号 | 严重程度 | 描述 | 状态 | 责任角色 |
|------|----------|------|------|----------|
| — | — | 未发现缺陷 | — | — |

## 验收标准覆盖

| 验收标准 | 结果 | 证据 |
|----------|------|------|
| AC-1.1 / AC-1.2 项目总览与单项目错误隔离 | 通过 | `snapshot.test.js`、页面工作台冒烟 |
| AC-2.1 / AC-2.2 项目详情与特殊 kind 降级 | 通过 | `snapshot.test.js`、页面工作台冒烟 |
| AC-3.1 / AC-3.2 接入诊断与配置异常 | 通过 | `config.test.js`、`index.test.js`、页面接入诊断冒烟 |
| AC-4.1 / AC-4.2 跨项目四 Tab 与 coordination 降级 | 通过 | `coordination.test.js`、页面跨项目冒烟 |
| AC-5.1 跨项目待办汇总只读 | 通过 | `project-index.test.js`、`snapshot.test.js`、页面工作台冒烟 |
| AC-6.1 部署入口 | 通过 | 页面部署冒烟 |
| AC-7.1 / AC-7.2 配置驱动接入与字段非法降级 | 通过 | `config.test.js`、`snapshot.test.js` |
| AC-8.1 / AC-8.2 60s 自动刷新与刷新错误隔离 | 通过 | `config.test.js`、`snapshot.test.js`、前端轮询契约检查 |

## 回归结论

- 设计 R1 Tester 遗留风险已关闭：`SnapshotError` 固定字段存在，配置异常和读取异常均有可断言降级路径，非活跃阻塞过滤已有 fixture 覆盖。
- 局部读取失败不阻断整批快照；缺状态列待办表降级为 `read-error`，不猜测完成态。
- 本轮未产生需 Developer 返工的阻塞项。
