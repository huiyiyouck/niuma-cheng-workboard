# v0.3 迭代记录

## 概览
- 目标：v0.3 小版本 —— 5 组 13 US：A 对话查看器（抽屉版 + 左菜单 + 选择即当前 + 1:N + 迭代时间轴 US-13 保留 v0.2）、B 匹配增强（detectRole + 兜底 General / 拖拽打标签 manualRole / Codex 多来源 / INDEX-git-history，依 BCR-013）、C 菜单 5→3、D 看板待办折叠 + 长文本、E 部署错误页。**不引入对被监控项目/业务数据的增删改查**（workboard 本地配置/元数据写入不算）。
- 开始时间：2026-07-12
- 关闭时间：—
- 最终状态：进行中
- 类型：标准迭代
- 当前阶段：实现（设计 §6 已回环订正定稿 2026-07-18；Developer 从 `workboard_dev` 续 R1：先落 §6.0 迁移引擎修复，再接 M-1 存储 + API + 前端）
- 收尾状态：未收尾

## 阶段门禁

> Bootstrap 初始化、迭代关闭确认和收尾归档不是迭代内的阶段，而是独立的非角色机制。触发时机和执行规则见 `docs/baseline/mechanisms.md`。
> 本区只放标准阶段标题：`PRD 阶段`、`设计阶段`、`实现阶段`、`部署就绪检查`。讨论记录、未来方向、停放性事项不得作为本区三级标题；应放入 `Review 记录`、`Change Notes` 或独立 ad-hoc。
> 阶段门禁状态只允许：`待Review / Review中 / 修改中 / 已定稿 / 阻塞 / 已跳过`。部署就绪检查使用独立状态：`待检查 / 检查中 / 通过 / 阻塞 / 已跳过`。门禁表最新轮次置于末行。

### PRD 阶段
| 轮次 | 产出方动作 | 指定 Review 方 | Review 结果 | 阶段状态 |
|------|------------|------------------|-------------|----------|
| R1 | PM 初版 **PRD + UI 原型图**（`v0.3-prd.md`：5 组 13 US（+US-13 迭代时间轴保留 v0.2）+ 验收 + 界面要点/ASCII 线框 + §7 实现指引；匹配闭环/1:N/Codex/沟通详情/待办折叠/错误页 · **原型图**：design 抽屉版对话查看器 + 会话下拉框 + 迭代时间轴，见 `docs/knowledge/ui/prototype-design-context.md`） | Architect（状态模型/1:N 迁移/manualRole/读 .git/Codex/沟通文档/detectRole 可行性）、Developer（前端翻译+后端从零+拖拽+菜单+错误页工程成本） | Architect ✅ 通过（M-1 中·1:N 存储载体待设计阶段定；L-1/L-2 低·US-12/US-8 后端 v0.2 已落地、PRD 表述过时建议 PM 订正）；Developer ✅ 通过（附议 M-1/L-1/L-2；另 D-1 低·§7 补「改造现有组件」路径、D-2 低·US-4 基线 7.5% 无出处，均建议 PM 订正不阻塞） | **已定稿**（R1 两方 ✅ 通过；PM 已订正 L-1/L-2/D-1/D-2 表述；M-1「1:N 存储模型」转设计阶段） |

### 设计阶段
| 轮次 | 产出方动作 | 指定 Review 方 | Review 结果 | 阶段状态 |
|------|------------|------------------|-------------|----------|
| R1 | Architect 产出 `v0.3-design.md`（M-1 存储模型：`manual_role` 落列 + `coalesce` 归类 + `session_mappings` 废弃 + `002` 迁移 · US-5 `.git` 历史区间重建 + 会话迭代标签 · API 契约：`/api/sessions` 增强 + `PUT/DELETE /api/sessions/role` + `/api/communications/detail` + 废弃 `/api/mappings` · 前端抽屉/菜单归位）+ ADR-0001 | Developer（存储迁移/API 契约/.git 解析/前端状态模型可实现性）、DevOps（生产 .git 可读性/DROP 表迁移风险/nginx 错误页） | Developer ✅ 通过（DEV-M1 中·§6 schema 双套 DDL 认知偏差、触发点应为「sync 时」非「启动」，实现前订正、不阻塞；DEV-L1/L2 低）；DevOps ✅ 通过（OPS-1 中·`002` 惰性自动应用无备份闸口，须把 `pg_dump` 定为 restart 前强制步骤；OPS-2 中·生产自监控 .git 路径须指真实克隆非部署目录；OPS-3/4 低；均不阻塞、部署阶段落，已给回滚策略） | **已定稿**（2026-07-18·R1 两方 ✅；Architect 订正 §6 DEV-M1/OPS-1/OPS-2 + §3.4 DEV-L2 + §7 DEV-L1）· **§6 回环订正**（2026-07-18·实现阶段发现迁移引擎 dollar-quoted bug → 新增 §6.0 方案 B，维持定稿，见 design 文末回环订正说明 + 本记录「设计 §6 回环」） |

### 实现阶段
| 轮次 | 产出方动作 | base_commit | head_commit | 指定 Review 方 | Review 结果 | 验证 | 阶段状态 |
|------|------------|-------------|-------------|------------------|-------------|------|----------|
| R1（后端） | Developer 落地：§6.0 迁移引擎修复（`executeMigration` 整文件交 PG 解析、退役 `splitSqlStatements`）+ §6.1 `002` 迁移（`manual_role` 落列 + 迁 `session_mappings.role` + DROP 表）+ `runSchemaDDL` 清理（DEV-M1 同轮）+ API 契约（`/api/sessions` 增强 `resolved_role`/`role` 过滤/`status` 重定义去 JOIN、新增 `PUT/DELETE /api/sessions/role`、新增 `GET /api/communications/detail` 不含 `sourcePath`、废弃 `/api/mappings` 三端点、`details` 去 JOIN 补 `resolved_role`；`iteration_label` 占位 null 待 R2）。前端 `/api/mappings` 残留已登记 INDEX 待办挂 R3 | be49001 | aca10cf | 待指定（R2 US-5 / R3 前端 / R4 菜单各轮完成后统一指定） | Architect ✅ 通过（详见 Review 记录·实现 R1~R5 Architect Review；设计一致/正确性/安全边界全过，参谋长席位空白确认接受）；DevOps ✅ 通过（部署侧五焦点核实，新增 OPS-5 手册 §8 复制漏 migrations 风险；均落部署就绪检查、不阻塞） | `workboard_dev` 全新库实测 `001→002` 链通过、`manual_role` 列在、`session_mappings` 已 DROP 且 `ensureSchema`（模拟 sync）不复活；API 端到端 15 项断言全绿（归类各分支/role 过滤/status/打标签 400·404/撤销回落/沟通全文·防穿越/mappings 404）；`npm test` 仅 2 既有真实数据耦合假失败（coordination/project-index，已登记待办） | ✅ 两方通过（Architect + DevOps） |
| R2（US-5） | Developer 落地迭代标签：新增 `parsers/iteration-history.js`（`parseCurrentIteration` INDEX 抽「当前迭代」/ `buildIntervals` 变化点切区间 / `matchIteration` `[startAt,endAt)` 落区间 / `readIterationIntervals` git `log --reverse`+`show` 只读、失败降级 `[]` / `getIterationIntervals` 按仓缓存 key=INDEX 最新 sha + 30s TTL）；`handleListSessions` 建 dirId→仓路径反查表、`iteration_label` 从占位改真实推断，`iteration_inferred` 恒 true，不可读→null 中性未归属（§4.3 降级） | aca10cf | ae2a944 | 同 R1（各轮完成后统一指定） | Architect ✅ 通过（详见 Review 记录·实现 R1~R5 Architect Review；设计一致/正确性/安全边界全过，参谋长席位空白确认接受）；DevOps ✅ 通过（部署侧五焦点核实 + OPS-5，落部署就绪检查、不阻塞） | TDD 10 用例全绿；真实仓区间与项目史实吻合（v0.1→空窗→v0.2→v0.2.1→v0.3）；端到端四时段断言全对（7-06→v0.2 / 6-30 空窗→null / 6-24→v0.1 / 今天→v0.3）；`npm test` 88/90（2 失败为既有假失败） | ✅ 两方通过（Architect + DevOps） |
| R3（A 组前端） | Developer 落地对话抽屉：新增 `ConversationDrawer`（右抽屉 ~72% + 左 5 角色菜单·会话数·拖拽放置 + 当前/历史·选择即当前 `currentByRole` 临时 state（US-11）+ 🏷 改归属（US-10）+ US-3 空态·General 池挑选打标 + `IterationBadge` 恒标推断（US-5）+ `SourceBadge`（US-12））；契约切换还 P1 债（删 mappings 三 hook/`SessionMapping` 类型，新增 `setSessionRole`/`clearSessionRole`，`useSessionList` 增 `role`，`ClaudeSession` 增四字段）；`SubProjectView` 按 `resolved_role` 1:N 分组、`RoleCard` 改角色入口、时间轴 `mappingsByRole` 派生角色→最新会话；`ConversationView` 单会话钻取全屏→抽屉容器；清理废弃组件五个。**设计空白决策**：M-1 无 `chief-of-staff` 枚举 → 参谋长席位改「生态根最新活跃会话」自动显示、删配置弹窗（供 Review 确认） | ae2a944 | 22e6a99 | 同 R1（各轮完成后统一指定） | Architect ✅ 通过（详见 Review 记录·实现 R1~R5 Architect Review；设计一致/正确性/安全边界全过，参谋长席位空白确认接受）；DevOps ✅ 通过（部署侧五焦点核实 + OPS-5，落部署就绪检查、不阻塞） | `vite build` 通过；Playwright 无头端到端断言全绿（抽屉/角色菜单/当前历史/徽章/空态）+ 控制台零错误；US-10 闭环实测（UI 打标→移组→左菜单计数 1→库落 `manual_role='PM'`→DELETE 撤销回 0）；截图与 PRD §3A 线框吻合 | ✅ 两方通过（Architect + DevOps） |
| R4（收尾轮） | Developer 落地 C/D/E 组：US-6 菜单 5→3（`NAV_ITEMS` 看板/项目会话/需求池，异常徽章挪看板；`DeployView`/`DiagnosticsView` 删除、内容降级为项目详情抽屉「接入 / 部署」tab = 新 `InfraDrawerTab`）；US-7 待办折叠（前 5 条 + 展开/收起 + footer hover peek + 单条 `line-clamp-2` + hover 全文浮层，两浮层 `createPortal` 到 body 防 `overflow-hidden` 裁剪、仅截断才弹）；US-8 沟通全文（条目点击 → 抽屉 `GET /api/communications/detail` + `react-markdown`+`remark-gfm` 只读渲染，404 空态）；US-9 前端（SPA 404 兜底 `NotFoundView` + 自包含静态 `50x.html`，nginx `error_page` 挂载归 DevOps） | 22e6a99 | 13d31be | 同 R1（统一指定） | Architect ✅ 通过（详见 Review 记录·实现 R1~R5 Architect Review；设计一致/正确性/安全边界全过，参谋长席位空白确认接受）；DevOps ✅ 通过（部署侧五焦点核实 + OPS-5，落部署就绪检查、不阻塞） | `vite build` 通过；Playwright 端到端断言全过（菜单 3 项/抽屉双 tab/折叠 5→6→收起/peek/长文本浮层 5/6 行·短文本不弹/四子 tab/沟通全文渲染/404/50x）+ 控制台零错误；`npm test` 88/90（2 既有假失败） | ✅ 两方通过（Architect + DevOps） |
| R5（Owner 本地验证反馈修复） | Owner 于本地测试环境（`localhost:5174`，`workboard_dev` 库 + 真实会话同步数据）全量验证，5 项反馈全部修复：① 抽屉顶栏徽章与标题同行对齐（`e6e1554`）；② 对话气泡 AI 回复按 Markdown 编译渲染（新共享组件 `MarkdownContent`，用户消息保持原文，`cfe92a1`）；③ 历史会话还原原型图**下拉框**形态（悬浮面板不占位、选中即当前并收起，`51fa76e`）；④ 拖拽修复（下拉「点外部关闭」透明层挡住角色菜单 drop，拖拽中撤层 + drop 后收起 + 空态池行补 draggable）；⑤ 生态根只读卡片钻取最小落地（公告板→需求池视图、agent-workflow→项目详情抽屉；深度详情留待办）（④⑤ = `a1685d8`）。**Owner 结论：本地验证通过** | 13d31be | a1685d8 | Architect + DevOps（Owner 新开会话冷启动，产出方不自审） | Architect ✅ 通过（详见 Review 记录·实现 R1~R5 Architect Review；设计一致/正确性/安全边界全过，参谋长席位空白确认接受）；DevOps ✅ 通过（部署侧五焦点核实 + OPS-5，落部署就绪检查、不阻塞） | 每项修复均 Playwright 实测（对齐中线 0px / 19 个 h2·80 个 strong 渲染无裸 `##` / 下拉选中切换收起 / 钻取两卡跳转）；`vite build` 通过 | ✅ 两方通过（Architect + DevOps） |

| R6（生产验收展示反馈 · 进行中） | Owner 生产验收功能通过后转达展示问题，Developer 逐项修复：① 看板项目卡被 INDEX 长状态撑爆 → `ProjectCard` 阶段/下一步/阻塞/kindSummary 全部 `line-clamp-2` 截断（完整内容进详情抽屉）+ 摘要剥 Markdown `**` 记号；② 顺手清迭代关闭检查遗留——`package.json` version bump `0.3.0`（`/api/health` 运行时读取、重部署即生效）；③ 修复 Developer 日志缺「结论」字段致 `roles.test.js` 真实文件用例新失败（文档格式合规，非代码回归）。**待 Owner 继续转达其余展示问题，齐后 DevOps 一次性重部署** | b01fe25 | bf52160 | 同前（修复完成后随迭代关闭由 Owner 生产复验背书） | 待反馈 | Playwright 实测四项目卡等高 332px（此前单卡撑爆）、星号剥净、两行省略号截断；`npm test` 回 88/90 基线（2 既有假失败） | 修改中 |

### 部署就绪检查
| 状态 | 目标环境 | 证据 | 阻塞项 |
|------|----------|------|--------|
| 通过（就绪） | 生产 `zijie`（阿里云 115.191.43.79）`workboard.huiyiyou.cloud` / `workboard-api-prod`:5181 / PG `workboard` | 2026-07-19 DevOps SSH 只读采集：① prod `.env`→`workboard` 库（非 dev）✅；② `schema_migrations`=v1、`session_mappings` 存在 **6 行**、`manual_role` 列未建 → `002` 待应用 ✅；③ config workboard 自身 path=`/root/Project/niuma-cheng-workboard` 真实克隆（**OPS-2 安全**）✅；④ 各被监控仓 `.git` root 可读（xiaobao/ai/agent-workflow/workboard；coordination 无 INDEX.md 属预期）✅；⑤ nginx 生产块已有 SPA fallback、缺 `error_page`（US-9 待加）；⑥ node v22/git 2.43/pg_dump/psql 齐备 ✅ | 无 |
| ✅ 已部署生产 + Owner 生产验收通过（2026-07-20） | 同上 | 2026-07-20 DevOps 生产部署完成（`b01fe25`）：**备份**代码 tar+`session_mappings`(6 行)+整库 13M+nginx conf；`git pull ad9b706..b01fe25` 快进；`npm --prefix frontend install`（新 `react-markdown`/`remark-gfm`）+`npm run build`（`index-DCZ5CYi_.js`）；`cp -a src/server/.` 递归含 `002.sql`+`iteration-history.js`（**OPS-5 未踩**）；`.env`/`config` 时间戳未变。**迁移**：`/api/sessions` 触发 `applyMigrations`（注意 `/api/health`/`/api/snapshot` 不触发，只 `handleListSessions` 族触发）→ `schema_migrations=1,2`、`manual_role` 建列、**6 行迁移**、`session_mappings` DROP；**chief-of-staff 悬垂值清理**（方案A `UPDATE 1`→现 5 行）；**DEV-M1** sync 后表未复活 ✅。**验证**：health `migrations:ok`；US-5 迭代标签有值（v0.1×12/v0.2×7/v0.6×6=生产 .git 可读实证）；resolved_role 分布健康；US-9 error_page 受控停后端实测 502→`50x.html` 兜底、`internal` 直接访问 404。**回滚**：备份目录 `/opt/workboard-prod/backup/v0.3-20260719-154503`（代码 tar 反向解 + 整库/表 `pg_dump` 恢复 + nginx conf.bak） | 无（遗留：health `version` 仍 `0.2.0`，`package.json` 未 bump，纯标签不影响功能，建议 Developer 后续 bump `0.3.0` + systemd unit 描述更新为 v0.3；非阻塞） |

## Review 记录

### PRD R1 · Architect（2026-07-14）
- **结论：✅ 通过**（技术可行性整体成立，无阻塞项）。完整结论见 `v0.3-prd.md` 文末 Review 记录。
- 核实代码现状后的关键发现：US-12（Codex 同步 + `source` 列）与 US-8（communications 读取）的后端管道**大部分已在 v0.2 落地**，PRD 对工程成本有高估。
- 问题：M-1（中）`manualRole` 存储载体与「1:N 迁移」语义歧义，`session_mappings` 现为严格 1:1，须设计阶段定明（建议 `manualRole` 落 `claude_sessions.manual_role` 列、归类 `coalesce(manual_role,detected_role,'General')`、`session_mappings` 整表废弃）；L-1/L-2（低）PRD §5/§7 对 US-12/US-8 后端现状表述过时，建议 PM 订正；L-3（低·体例）两个空 `## Review 记录` 标题建议合并。
- 均不阻塞 PRD 定稿；待 Developer R1 Review 完成后由 PM 判断是否进「修改中」订正表述，再定稿进设计阶段。

### PRD R1 · Developer（2026-07-14）
- **结论：✅ 通过**（可实现性整体成立，无阻塞项）。完整结论见 `v0.3-prd.md` 文末 Review 记录。
- 独立读代码核实 10 个工程成本维度均可落地；**附议** Architect M-1（`session_mappings` 严格 1:1、`claude_sessions` 无 `manual_role` 列，是实现直接阻碍）与 L-1/L-2（`codex-parser.js` 完整非 stub、`source` 列已存在、`communications` 已 `readFile` 全文——US-8/US-12 后端成本高估属实）。
- 另提两条低 severity：D-1（§7 实现指引宜补「A 组=改造 v0.2 现有内联组件，非从 design 从零翻译」）、D-2（US-4 验收基线「~7.5%」无出处/口径，验收无法对照，建议 PM 标明）。均不阻塞。
- 附四条设计阶段实现接力提示：US-5 git 读取封装/时间窗匹配为 B 组大头；US-8 全文透出宜走 by-id 接口（避免 snapshot 膨胀）；US-6 须理清 `App.tsx`/`EcosystemView.tsx` 两套视图归位；US-7 `overflow-hidden` 浮层 portal。
- 两方 R1 均已通过，无阻塞。下一步由 PM 判断是否进「修改中」订正 §5/§7/US-4 表述（M-1/D-1/D-2/L-1/L-2），再定稿进设计阶段。

### 设计 R1 · Developer（2026-07-18）
- **结论：✅ 通过**（存储模型/API 契约/.git 解析/前端状态模型均可落地，无阻塞项）。完整结论见 `v0.3-design.md` 文末 Review 记录。
- 独立读代码核实：M-1 路径 A 决策认同（ADR-0001 分析扎实）；`detected_role` 入库确为字符串 `'Unknown'`（`claude-sync.js:397`），`nullif(detected_role,'Unknown')` 兼容假设成立；全新库迁移链健壮（`001` 建表 → `002` UPDATE→DROP，版本顺序保证）；API 契约照现有硬匹配路由风格可无歧义新增/改造。
- **DEV-M1（中，不阻塞）**：§6 对 schema 初始化机制认知偏差——`runSchemaDDL`（`db.js:81`）**不在 HTTP/启动路径**，只被 `claude-sync.js:438/497` 在 **sync 时**调；设计说「每次启动重建」触发点错（应为「每次 sync」），且未点破 `migrations`(001) 与 `runSchemaDDL` 是两套重复 schema 真源。建议 Architect 订正 §6 触发点表述；实现最小闭环=删 `runSchemaDDL` 的 session_mappings 段（`db.js:125-145`），可选根治=让 `claude-sync` 改走 `applyMigrations` 退役内联 DDL。
- DEV-L1（低·US-5 每 commit 一次 `git show` 子进程首次偏慢，可用 `git log -p` 优化）、DEV-L2（低·`/api/communications/detail` 出参 `sourcePath` 绝对路径不宜透前端）。
- 下一步：DevOps R1 Review（生产 .git 可读性/DROP 表迁移风险/nginx 错误页）；两方通过后由 Architect 判断是否进「修改中」订正 §6（DEV-M1），再定稿进实现阶段。

### 设计 R1 · DevOps（2026-07-18）
- **结论：✅ 通过**（生产 .git 可读性/DROP 表迁移风险/nginx 错误页均有合理交代或合理下放到部署就绪检查，无阻塞高严重度问题）。完整结论见 `v0.3-design.md` 文末 Review 记录。
- 基于真实部署事实核实：DB 引擎确为 **PostgreSQL**（`pg_dump` 回滚策略适用）；迁移**事务原子**（`002` ALTER+UPDATE+DROP 整体 `BEGIN…COMMIT`，无半迁移态）；`.git` 降级路径健壮（不可读→标签 null 不崩）；生产被监控项目仓 `/root/Project/niuma-cheng-*` 为真实克隆、root 可读。已补齐设计点名的**回滚策略**（restart 前强制 `pg_dump session_mappings` + 健康检查触发回滚 + 反向 cp 上一版）。
- **OPS-1（中，不阻塞）**：`002` 惰性自动应用（`applyMigrations` 每 handler 首次请求触发，含 `/api/health`），`systemctl restart` 后第一笔流量即自动 `DROP`，无人工备份闸口→须把 `pg_dump` 定为部署 runbook 里 restart **之前**的强制步骤（部署就绪检查会写进清单）。
- **OPS-2（中，不阻塞）**：生产自监控 .git 陷阱——生产 config 绝对路径若把 workboard 自身指向部署目标 `/opt/workboard-prod/app`（`cp -a` 非 git 目录），US-5 对 workboard 自己恒 null（静默漏测）；须确保各项目 path 指真实 git 克隆。
- OPS-3（低·US-9 错误页规格缺失，静态页来源/路径/两套 nginx 站点/`error_page` 指令待部署阶段补，且须先核实生产 nginx 现块是否含 SPA fallback）、OPS-4（低·git dubious-ownership 备查，当前全 root 无虞）。
- 交叉印证 Developer DEV-M1：确认「DROP 后 sync 复活空表」有部署可观测性（`schema_migrations` 已记 002 却表回来→静默不一致），部署就绪检查将加「迁移后跑一次 sync 复查 `session_mappings` 仍不存在」一条。
- 下一步：**两方 R1 均 ✅ 通过、无阻塞**；待 Architect 判断是否进「修改中」订正 §6（DEV-M1，触发点表述）后定稿进实现阶段。DevOps 侧的 OPS-1~4 均落部署就绪检查/实现阶段，不要求设计正文改动即可定稿。

### 设计 §6 回环 · Developer 实现阶段发现（2026-07-18）

**触发**：实现阶段 R1 搭隔离开发环境（新建 `workboard_dev` 库）时，`applyMigrations` 跑 `001` 报 42601 语法错，暴露迁移引擎缺陷 + 设计 §6 假设瑕疵。**Owner 拍板回设计阶段**由 Architect 订正 §6，R1 暂停待订正（设计门禁待 Architect 自行改「已定稿」→「修改中」，本条为 Developer 发现移交，不代改门禁/设计正文）。

**发现（给 Architect 订正 §6 的依据，证据充分）：**
- **migrations 引擎 DO `$$` 块 bug**：`migrations.js` `splitSqlStatements`（`:100-143`）按 `;` 拆语句、**不识别 dollar-quoted 块**；`001_init.sql` 的 session_mappings 约束用 `DO $$ BEGIN IF NOT EXISTS(...) THEN ALTER TABLE ...; END IF; END $$;`，内部分号被误拆 → 全新库执行 `001` 语法错（42601）。
- **生产为何没暴露**：生产 `workboard` 库 `schema_migrations` **已记 version 1**（历史某时点写入），`applyMigrations` 跳过 `001`，故 bug 只在**全新库/重建库**（如 `workboard_dev`）暴露。
- **设计 §6 假设被证伪**：§6「全新库靠 `001` 建 `session_mappings` → `002` DROP 收口」不成立——全新库 `001` 跑不起来、`applyMigrations` 直接失败，`002` 的 `UPDATE ... FROM session_mappings` 无表可依。
- **生产数据事实**（供订正知情）：生产 `session_mappings` **6 行真实手动映射**，`002` 步骤2 须正确迁进 `manual_role`（不可简单 DROP 丢数据）；`claude_sessions` 现无 `manual_role`（002 未跑），`detected_role`/`source` 已在。

**建议 §6 补充方向**（供 Architect 定，不替其决策）：实现阶段须先让迁移引擎支持 dollar-quoted 块（修 `splitSqlStatements`，或改 `executeMigration` 执行方式），使全新库 `001→002` 链成立；`002` 本身纯 `ALTER/UPDATE/DROP`、不含 DO 块、不受影响。属迁移引擎健全化，是 `002` 可靠落地前提。

**环境已就绪待续**（R1 隔离准备，不丢）：本地 `.env` 已同步生产现行 PG 密码（Owner 授权取自 `/opt/workboard-prod/app/.env`，旧密码 18 字符已失效、现行 19 字符）、新建空库 `workboard_dev`、`.env` 切至 dev 库；生产 `workboard` 零触碰。§6 订正定稿后 R1 从此环境续。

**下一步**：Owner 新开会话「你是 Architect」进「修改中」订正设计 §6，再回实现阶段续 R1。

### 设计 §6 回环 · Architect 订正回应（2026-07-18）
- **核实**：读 `migrations.js:100-143`（`splitSqlStatements` 只跟踪单/双引号+注释、不识别 `$$` 块）+ `001_init.sql:56-60`（DO 块内多分号）——Developer 诊断**完全属实**，根因确认（全新库 `001` 被误拆致 `42601`，`applyMigrations` 中断，`002` 轮不到；生产因 `schema_migrations=v1` 跳过 `001` 故未暴露）。
- **订正方案（方案 B，Architect 定）**：`executeMigration` 改为整个 `.sql` 文件单次 `client.query(sql)`、交 PostgreSQL 服务端解析、**退役 `splitSqlStatements`**（不再手搓 SQL 拆分器）；保留外层 `BEGIN/COMMIT` 事务；约束迁移文件不用参数占位符。**未选方案 A（给拆分器打补丁支持 `$$`）**——手搓拆分器是本类 bug 温床，B 更根治。设计新增 **§6.0**，§7 补取舍一条，§6.1 标注 `002` 依赖 §6.0。
- **两条路径正确性**：全新库引擎修复后 `001`（DO 块）→ `002`（空表迁移 0 行 + DROP）成立；生产 `002` 无 DO 块、6 行数据由步骤 2 迁移进 `manual_role`，引擎修复对生产 `002` 无害（+ OPS-1 `pg_dump` 兜底）。
- **未再走正式 Review**：属实现发现→回设计的前提补全，非架构方向变更，DevOps 视角（数据安全/原子性）不受方案 B 影响。订正即定稿。
- **下一步**：Owner 新开会话「你是 Developer」从 `workboard_dev` 隔离环境续实现 R1——**先落 §6.0 引擎修复**（改 `executeMigration` + 退役 `splitSqlStatements`），跑通全新库 `001→002`，再接 M-1 存储 + `runSchemaDDL` 清理 + 归类 + API + 前端。

### 实现阶段 R1~R5 · Architect Review（2026-07-19）

**结论：✅ 通过**（实现与设计高度一致、关键正确性与安全边界均到位，无阻塞项）。独立读码核实五轮实现，未发现需打回的问题；一处设计空白（参谋长席位）Developer 的补充决策我确认接受。DevOps 侧 Review 待另行。

**逐项核实（实际读码，非过场）：**

- **§6.0 迁移引擎修复（回环方案 B）**：`executeMigration`（`migrations.js:75-98`）已改为整个 `.sql` 文件单次 `client.query(sql)` 交 PG 服务端解析、保留外层 `BEGIN/COMMIT/ROLLBACK`，`splitSqlStatements` **已彻底删除**（全文件无残留定义/调用）。完全落地我定的方案 B，注释准确引 §6.0。
- **§6.1 `002` 迁移**：`002_session_role_model.sql` = `ALTER ADD manual_role` + 幂等 `UPDATE …FROM session_mappings`（`manual_role IS NULL` 才写）+ `DROP TABLE IF EXISTS`，含 OPS-1 `pg_dump` 提醒注释。符合设计，全新库迁 0 行 / 存量库迁 6 行均正确。
- **DEV-M1 `runSchemaDDL` 清理**：`db.js` 的 `session_mappings` 建表/约束/索引段已删（仅留一句废弃注释 `:124`），`runSchemaDDL` 不再复活表。DEV-M1 与 `002` 同轮落地。
- **归类 SQL**：`RESOLVED_ROLE_SQL = COALESCE(s.manual_role, NULLIF(s.detected_role, 'Unknown'), 'General')`（`index.js:139`）与设计 §2.1 逐字一致，贯穿列表/详情/打标签返回。
- **API 契约全套**：`/api/sessions` 增强（`role` 过滤**参数化**防注入 `:194`、`status` 重定义去 `session_mappings` JOIN `:186-189`、`resolved_role` 填充、`iteration_label` 真实推断）；`PUT /api/sessions/role` **枚举校验**（`VALID_ROLES.includes` → 400，`:286`）；`DELETE …/role` 撤销回落；`GET /api/communications/detail` 出参不含 `sourcePath`（DEV-L2 落实）；`/api/mappings` 三端点已从路由表移除（废弃落实）。
- **安全边界**：① 沟通接口**防目录穿越**——`readCommunicationDetail`（`coordination.js:152`）`if (!id || /[/\\]|\.\./.test(id)) return null`；② US-5 `.git` 用 `execFile` 数组参数（**非 shell 拼接**，防命令注入）+ 只读 `git log/show` + 失败降级 `[]`，守住「只读、不触工作区、不写」边界；③ SQL 全参数化。
- **US-5 迭代重建**：`iteration-history.js` 区间匹配为**半开区间 `[startAt,endAt)`**（`:46` `t >= start && t < end`，边界不重叠正确）、变化点切区间同值不切、最新 `endAt=null`、按 `repo+INDEX 最新 sha`+30s TTL 缓存；`iteration_inferred` 恒 true。符合设计 §2.3/§4.3。
- **前端**：R3~R5 抽屉/菜单 5→3/折叠/沟通全文/错误页，Owner 已本地全量验证通过 + 各轮 Playwright 端到端全绿 + 截图对 PRD §3A 线框，符合度由 Owner 验证背书，Architect 侧确认数据契约对接正确（`resolved_role` 分组、`currentByRole` 前端临时、`role`/`setSessionRole`/`clearSessionRole` 对应新端点）。

**设计空白确认（Review 需拍板项）：**
- **参谋长席位**：M-1 角色枚举（`VALID_ROLES` 5 项）无 `chief-of-staff`，Developer 对生态根（`ecosystem-root`）改用 `ChiefOfStaffCard`（`EcosystemView.tsx:221`）取「最新活跃会话」自动显示 + 钻取 + 空态，删配置弹窗。**Architect 确认接受**：参谋长不属项目内工作流角色体系、生态根非标准工作流项目，硬塞枚举不合理；「最新活跃自动显示、免配置」符合 workboard 只读定位，且为独立分支不破坏其他项目角色菜单。**建议（非阻塞·未来项）**：若日后要把「参谋长」作一等角色纳入归类，需扩展 `VALID_ROLES` + `detectRole`，本迭代不做。

**非阻塞观察（记录，不打回）：**
- DEV-L1（Developer 自己提过）：`readIterationIntervals` 每 commit 一次 `git show` 子进程，未采纳 `git log -p` 单进程优化；有 sha+TTL 缓存兜底、真实仓量级可接受，留作后续优化。
- `npm test` 88/90：2 项失败为既有真实数据耦合假失败（`coordination`/`project-index`，已登记跨任务待办），非本次回归。
- **事故记录**（Developer R1 日志坦诚记载）：实现中段因终端乱码误读、虚构过幻影 commit 与框架事故推断，事后 `git cat-file` 证实仓库历史全程线性完好、零丢失——**不影响最终代码正确性**，教训已记录。Architect 复核 HEAD 线性、当前代码一致，确认无实质影响。

**下一步**：DevOps 侧实现 Review（部署影响：`002` DROP/`pg_dump` 闸口、生产 `.git` 可读性、nginx `error_page`）；两方通过后进部署就绪检查。

### 实现阶段 R1~R5 · DevOps Review（2026-07-19）

**结论：✅ 通过**（部署侧五焦点逐项读码/读产物核实，实现可安全发布，无阻塞项。所有部署风险均属发布流程约束、非代码缺陷，落部署就绪检查清单）。与 Architect 2026-07-19 实现 Review 互补——正确性/安全边界 Architect 已覆盖，本 Review 只审部署方式/发布风险/回滚/环境。新增一条发布流程风险 **OPS-5**。

**逐项核实（实际读码/读产物）：**

1. **§6.0 迁移引擎 + OPS-1 DROP 闸口**：`executeMigration`（`migrations.js`）已改整文件 `client.query(sql)` + 外层 `BEGIN…COMMIT/ROLLBACK`，`splitSqlStatements` 已彻底删除——`002` 的 `ALTER+UPDATE+DROP` **单事务原子**、无半迁移态，我 R1 认可的原子性保住。`002.sql` 纯字面量无参数占位符（符合 §6.0 simple-query 约束），注释内嵌 OPS-1 `pg_dump` 提醒。**惰性自动应用不变**（`applyMigrations` 每 handler 首次请求触发）→ 发布风险仍是「restart 后第一笔流量即自动 DROP」，`pg_dump session_mappings` 须为 restart 前强制步骤（生产 6 行真实映射由步骤 2 迁进 `manual_role` 不丢数据，pg_dump 兜底不可逆的 DROP）。
2. **DEV-M1 `runSchemaDDL` 清理（发布可观测性）**：`db.js` 的 `session_mappings` 建表/约束/索引段已删（留废弃注释 `:124`），DROP 后 sync 不复活空表。部署就绪检查实测：迁移后跑一次 `/api/sync` 再查 `to_regclass('session_mappings') IS NULL` 验证清理生效。
3. **生产 .git 可读性（OPS-2）**：`iteration-history.js` git 用 `execFile` 数组参数（非 shell 拼接、防注入）+ 只读 `log/show` + 失败降级 `[]`——不可读时迭代标签全 null 不崩，边界守住。生产各仓 `/root/Project/niuma-cheng-*` 真实克隆 root 可读（前提成立）；OPS-2 自监控陷阱须部署就绪检查核对生产 `projects.config.json` 中 workboard 自身 path 指真实克隆、非部署目标 `/opt/workboard-prod/app`。
4. **US-9 错误页 nginx 挂载（OPS-3 收口）**：`frontend/public/50x.html` 完全自包含（内联 CSS、零外链、通用 `5xx` 覆盖 500/502/503/504），构建进 `frontend/dist/50x.html`，随前端产物 `cp` 到 prod web root `/var/www/workboard.huiyiyou.cloud/50x.html`——后端挂掉时 nginx 可独立直出。SPA 404 由 `NotFoundView`（`App.tsx:1385`）前端接管。nginx 侧我在部署时加 `error_page 500 502 503 504 /50x.html;` + `location = /50x.html { internal; }` 到两套站点。产物已就位，nginx 指令归 DevOps 部署落。
5. **新依赖发布影响（⑤）**：前端新增 `react-markdown ^10.1.0` + `remark-gfm ^4.0.1`（US-1/US-8 Markdown 渲染），后端**无**新增运行时依赖。发布须 `npm --prefix frontend install` 后再 `npm run build`，否则 build 失败/产物过时。

**新发现 OPS-5（中·发布流程，非代码缺陷）**：部署手册 §8 复制步骤仍写 `cp -a src/server/*.js`（只复制 `.js`，**漏 `migrations/002_session_role_model.sql`**）。2026-07-07 ops 已记「须改用 `cp -a src/server/.` 递归、否则漏 `migrations/`」，但手册正文未订正。**v0.3 若照手册 §8 stale 步骤部署，`002.sql` 不进生产 → `applyMigrations` 不跑 002 → `manual_role` 列缺失 → 所有 `COALESCE(s.manual_role,...)` 查询 500**。→ 部署必须 `cp -a src/server/.`（递归含 `migrations/` + 新 `parsers/iteration-history.js`），并订正手册 §8（部署时一并做）。

**部署就绪检查清单（DevOps 执行部署阶段逐项落，此处预登记）：**
1. restart 前 `pg_dump -t session_mappings workboard > 备份`（OPS-1，强制）。
2. 生产 `.env` 确认指向生产 `workboard` 库（**非 `workboard_dev`**）——开发用隔离 dev 库测破坏性迁移，发布勿带 dev 配置。
3. `cp -a src/server/.` 递归复制（含新 `002.sql` + `parsers/iteration-history.js`）（OPS-5）。
4. `npm --prefix frontend install` → `npm run build`（新 Markdown 依赖，⑤）。
5. nginx 两套站点加 `error_page 500 502 503 504 /50x.html;` + `location = /50x.html { internal; }`（US-9），`nginx -t` + reload。
6. 生产 `projects.config.json` 各项目 path 指真实 git 克隆、含 workboard 自身（OPS-2）。
7. 部署后：`/api/health` 返回 `migrations:"ok"`；跑一次 sync 复查 `session_mappings` 不复活（DEV-M1）；抽查若干会话 `iteration_label` 有值（.git 可读实证）。
8. 回滚：健康检查失败 → 反向 cp 上一版 + restart；数据从步骤 1 `pg_dump` 恢复（`schema_migrations` 已记 002，回旧代码不重跑迁移）。

**下一步**：两方（Architect + DevOps）实现 Review 均 ✅ 通过、无阻塞 → 进部署就绪检查（DevOps 执行上述清单 → 生产部署 → 生产复验 → Owner 验收）。

| Change Note | 状态 | 变更级别 | 受影响角色 | 执行状态 | 归档状态 |
|-------------|------|----------|------------|----------|----------|

## Git 关键节点

| 节点 | commit | 说明 |
|------|--------|------|

## 迭代关闭归档
- Owner 验收（真源记录，未验收 / 打回则不得关闭）：**通过**（2026-07-20 Owner 真实网络验收「验证完成，可以跑了」+ 生产可访问确认「可以访问」——消除部署知识记载的公司网络 80/443 拦截风险）
- Developer 自测结论（证据齐备）：证据齐备 — 见实现阶段 R1~R5 各轮「验证」列（后端 API 15 项端到端断言、US-5 TDD 10 用例、前端 Playwright 全绿+控制台零错误、US-10 打标闭环、`npm test` 88/90 其 2 为既有真实数据耦合假失败已登记待办）
- 关闭结论：**暂不关闭**（迭代关闭检查 2026-07-20 DevOps 执行，技术层 9 项全过、判定「技术可关闭」；但 **Owner 决定不关闭**——生产验收功能通过后又发现若干**展示问题**，要找 Developer 修改后再评估关闭）
- 未关闭原因或延期项：Owner 提出展示问题待 Developer 修复（具体项由 Owner 转达 Developer；预计走新一轮实现 fix 或 Change Note，类似 R5 的 Owner 反馈修复；修复+重部署后再执行关闭检查）
- 已生成 summary：未（暂不关闭，待展示问题修复收口后生成）
- 已更新 `docs/progress/INDEX.md`：—
- 已更新当前角色日志：—
- 其他角色待补充/确认：—
- 已更新知识库：—
- 遗留问题下一步：—

## 关联
- PRD：[v0.3-prd.md](v0.3-prd.md)
- BCR-013 评估游标（B 组方案依据）：agent-workflow `docs/progress/ad-hoc/2026-07-12-bcr-013-session-role-iteration-marker.md`
