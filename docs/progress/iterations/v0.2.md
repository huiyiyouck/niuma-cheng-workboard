# v0.2 迭代记录

## 概览
- 目标：新增「项目会话」视图，打通牛马程生态各项目 Claude Code 会话与角色的映射关系，支持查看对话内容和迭代时间轴钻取
- 开始时间：2026-07-04
- 关闭时间：
- 最终状态：进行中
- 类型：标准迭代
- 当前阶段：部署（v0.2 生产部署 ✅ 完成，等待 Owner 验收）
- 收尾状态：未收尾

## 阶段门禁

> Bootstrap 初始化、迭代关闭确认和收尾归档不是迭代内的阶段，而是独立的非角色机制。触发时机和执行规则见 `docs/baseline/mechanisms.md`。

### PRD 阶段
| 轮次 | 产出方动作 | 指定 Review 方 | Review 结果 | 阶段状态 |
|------|------------|------------------|-------------|----------|
| R1 | PM 初版 `v0.2-prd.md` | Architect / Developer | Architect ❌ 不通过（Claude Code 会话数据 API 可用性未验证，高风险阻塞）；Developer 通过（带高风险实现阶段验证） | Review中 |
| R2 | Architect 完成 Spike-001（Claude Code 会话数据读取可行性验证，JSONL 格式完全可读）；PRD 补充 §6.3 会话缓存 / §6.4 摘要降级 / §6.5 生产数据源同步 / US-15-17 | Architect / Developer | Architect ✅ 通过（高风险解除，新增内容架构合理）；Developer ✅ 通过（R2，新增内容工程可行） | 已定稿 |

> Owner 拍板全部待澄清问题：①角色自动识别作为辅助标签非核心路径；②生产数据源同步脚本由 DevOps 在部署阶段落地；③General 角色保留映射配置但默认折叠。

### 设计阶段
| 轮次 | 产出方动作 | 指定 Review 方 | Review 结果 | 阶段状态 |
|------|------------|------------------|-------------|----------|
| R1 | Architect 创建 `v0.2-design.md`（数据模型 / API 契约 / 核心流程 / 前端影响 / 迁移兼容） | Developer / PM | PM ✅ 通过（2 中严重度建议补充）；Developer 原通过 → 补充复审 ❌ 不通过（DR-1：时间轴解析策略三重不匹配，高） → Architect 修正 5 项（1 高 + 4 中） → Developer R2 ✅ 通过（1 文档一致性问题后置） | 已定稿 |

### 实现阶段
| 轮次 | 产出方动作 | base_commit | head_commit | 指定 Review 方 | Review 结果 | 验证 | 阶段状态 |
|------|------------|-------------|-------------|------------------|-------------|------|----------|
| R2 | Developer 本机验收多轮修复 + 会话源扩展：角色自动识别（设计 §4.4 前置处理）、阶段状态解析多批（状态词表兼容 + 非标准阶段降级为附加记录）、参谋长席位/详情接口参数、rebuild 重复计数、**IRC-002 双数据源同步**（本机 + 服务器 rsync 镜像，`claude_project_id` 支持数组）、**IRC-003 Codex 会话源兼容**（仅 Claude Code + Codex 两种，不做通用适配层）、配置驱动白名单过滤（只入库配置项目目录）、`ensureSchema` 同步期锁竞争修复；含 Developer code-review 自审（发现并修 `cwdToProjectId` 点号编码潜伏 bug） | 435dd76 | 2073671 | Architect / DevOps（独立复核）+ Owner（验收） | ✅ 通过（R2-2：Architect + DevOps 均通过；DH-2 延后 v0.3） | npm test 79/79 通过 + 本机手测 | 已定稿 |

### 部署就绪检查
| 状态 | 目标环境 | 证据 | 阻塞项 |
|------|----------|------|--------|
| ✅ 通过 | 生产环境（`workboard.huiyiyou.cloud`） | 后端 systemd `workboard-api-prod`（v0.2，绑定 `127.0.0.1:5181`，enabled + active）；PostgreSQL 16 配置完成（`workboard` 数据库 + `postgres` 用户密码认证）；生产 `.env` 配置完成；前端 `/var/www/workboard.huiyiyou.cloud` 更新为 v0.2 构建产物；本机 API `/api/health` 返回 `{"status":"ok","db":"ok","version":"0.2.0"}`；本机 SNI HTTPS 入口返回 200；服务内存 105MB（会话同步正常） | 无 |

## 自测证据（R2，Developer，2026-07-06）

> 测试阶段并入实现自测；本节留 R2 验收修复轮的证据，供 Architect/DevOps 独立复核与 Owner 验收。

**自动化（Developer R2 自测）**：`npm test` **76/76 通过**（Node 内置 `node --test`）。本轮新增/更新用例：`session-meta`（标题提取 + 角色识别 10 例）、`iteration-record`（阶段门禁解析 12 例，此前无测试）、`codex-parser`（Codex 解析 + cwd 编码 5 例）、`claude-sync`（远程源解析 + 白名单收集 4 例）；同步更新 2 个「跟随真实真源」的用例（coordination BCR 10→11、INDEX 待办 0→2）。

**自动化（R2-1 Architect 复核复跑，2026-07-06）**：`npm test` **78/78 通过**。复跑时发现 `src/server/parsers/project-index.test.js` 中读取真实 `docs/progress/INDEX.md` 的断言仍期望 `blocked === null`，而当前 INDEX.md 已记录 H-1~H-3 阻塞项；已同步更新该用例断言以匹配真源状态。

**本机手测（四维，逐项通过）**：
- 正常路径：5 个配置项目会话列表按项目目录严格过滤，Claude Code + Codex 混合展示（xiaobao 56 / ai 14 / workflow 14 / coordination 6 / workboard 11），角色识别（你是运维→DevOps、你是产品→PM 等）正确。
- 边界/异常：SSH 隧道断连时会话接口显式报错（前端「加载失败+提示检查隧道」，非静默「无匹配会话」）；rsync 单源失败不阻塞同步；Codex 无 session_meta 文件被拒绝入库。
- 状态转换：阶段状态解析覆盖 xiaobao v0.5/v0.6/v0.6.1、ai v0.1、workboard v0.2 多种历史门禁表写法（已定稿/部署通过/已Review/✅验收/已跳过）与非标准标题（UI 增强方向→附加记录）。
- 配置驱动：白名单外历史误入库会话清理（194→116，残留 0）；「xiaobao-server」独立部署目录越界项已剔除。
- 同步性能：`ensureSchema` memo 化后，全量同步进行中 `/api/snapshot` 稳定 200、亚秒响应（修复前锁竞争超时 000）。

## Review 记录

> 详见 `v0.2-prd.md` 和 `v0.2-design.md` 的 Review 记录区；本表汇总各阶段 Review 结论。

### R2 — Architect 实现 Review（2026-07-06）

- **结论**：❌ **不通过**。实现功能完整、测试 76/76 通过、IRC-001/002/003 已落地，但存在 3 项高严重度阻塞问题：① `src/server/config.js` 未实现 v0.2 配置校验（`level`、新增 `kind`、`ecosystem.root_session_id` 禁止存在）；② `projects.config.json` 仍保留 `ecosystem.root_session_id: null`，违反设计文档 §6.1 迁移要求；③ `session_mappings` 表缺少 `(project_id, role)` 唯一约束，违反设计文档 §2.2 业务规则。另有多项中严重度问题需修正或补充文档。高严重度问题修正后重新 Review。
- **Review 范围**：实现代码（`src/server/`、`frontend/src/app/`、`projects.config.json`、`package.json`）与 `v0.2-design.md` 一致性、已落地 IRC 变更合理性、v0.1 兼容性。
- **已确认可接受项**：
  - IRC-001 SQLite → PostgreSQL：Owner 已批准，`v0.2-design.md` 已记录，`.gitignore` 已处理 `.env`，部署文档已说明 SSH 隧道/直连方案。
  - IRC-002 双数据源同步 + rsync 镜像：`v0.2-design.md` 已记录，`projects.config.json` 已补齐 `claude_project_id` 数组，白名单过滤逻辑存在。
  - IRC-003 Codex 兼容：`v0.2-design.md` 已记录，配置驱动白名单过滤已实现，明确不做第三种源。
  - 时间轴解析器 `iteration-record.js`：与 `v0.1.md`/`v0.6.md` 真实结构匹配，多轮门禁取最后一行、状态映射完整、非标准阶段降级为附加记录。
  - 会话同步引擎增量策略：首次全量、定时增量、手动刷新、`file_mtime` 变更 + 文件截断 rebuild、同步期 `ensureSchema` memo 化。
  - 测试覆盖：`npm test 76/76` 通过，核心解析器和白名单逻辑有单测。
- **高严重度问题（阻塞）**：

| # | 问题 | 位置 | 说明 | 建议修正 |
|---|------|------|------|----------|
| H-1 | `config.js` 未实现 v0.2 配置校验 | `src/server/config.js` | ① `PROJECT_KINDS` 仍是旧 4 个枚举，缺少 `framework`/`business-primary`/`business-service`/`tool`；② 没有 `level` 字段校验；③ 没有 `ecosystem.root_session_id` 存在时报错退出的校验 | 扩展 `PROJECT_KINDS`，新增 `level` 校验，启动时若 `ecosystem.root_session_id` 存在则抛 `ConfigLoadError` |
| H-2 | `projects.config.json` 仍保留 `root_session_id` | `projects.config.json` 第 5 行 | 设计文档 §6.1 明确：config 手动一次性删除 `ecosystem.root_session_id`，后端启动校验存在则报错退出 | 删除 `ecosystem.root_session_id` 字段；如保留也必须在 config.js 中放宽校验，但设计契约要求删除 |
| H-3 | `session_mappings` 表缺少 `(project_id, role)` 唯一约束 | `src/server/db.js` | 设计文档 §2.2：`PRIMARY KEY (project_id, role_id)`，保证每个项目的每个角色只能映射一个会话；实现是 `id SERIAL` + `session_id UNIQUE`，允许同一项目同角色多映射 | 添加 `UNIQUE(project_id, role)` 约束，或应用层 upsert 时先删除旧映射 |

- **中严重度问题（不阻塞但需修正）**：

| # | 问题 | 位置 | 说明 | 建议修正 |
|---|------|------|------|----------|
| M-1 | 前端未引入 Markdown 渲染依赖 | `frontend/package.json`、`frontend/src/app/components/EcosystemView.tsx` | 设计文档 §1.1/§5.4 明确选型 `react-markdown` + `remark-gfm` + `rehype-highlight`；当前 `ConversationView` 纯文本渲染，代码块/表格/列表格式丢失 | 安装依赖并在 `MessageBubble` 中用 `react-markdown` 渲染 assistant 消息 |
| M-2 | API 路径与设计文档不一致 | `src/server/index.js` | 设计：`GET /api/sessions/:sessionId/messages`、`GET /api/projects/:projectId/timeline`；实现：`GET /api/sessions/details?id=`、`GET /api/timeline/versions?project_id=` | 两种风格都可用，但需统一文档；建议更新 `v0.2-design.md` §3 API 契约或改实现路径 |
| M-3 | `ecosystem.root_path` 字段未使用 | `projects.config.json`、`src/server/config.js` | 设计文档 §2.1 中 `ecosystem` 有 `root_path`；实现未使用 | 确认是否废弃；如废弃则更新 design.md 和 config，如保留则实现使用 |
| M-4 | IRC 变更未走正式 Change Note 文件 | `docs/progress/change-notes/` | IRC-001/002/003 仅记录在 `v0.2-design.md` 和 `v0.2.md`，无独立 Change Note 文件 | 可选，但建议按工作流补 `docs/progress/change-notes/irc-001-postgresql.md` 等 |
| M-5 | `session_mappings` 表缺少 `session_title` 冗余字段 | `src/server/db.js` | 设计文档 §2.2 有 `session_title` 字段，用于避免每次查询会话列表；实现没有 | 评估是否需要；如不需要更新 design.md，如需要则加列并在保存映射时写入 |

- **低严重度观察（不阻塞）**：
  - API 响应结构与设计文档略有差异（如会话列表包装为 `{items,total,limit,offset}`），不影响功能。
  - `ChiefOfStaffDialog` 硬编码 `ecosystem-root` 和 `chief-of-staff`，当前设计允许参谋长作为特殊映射处理。
  - 对话视图消息对齐方向与常规 IM 相反（assistant 在左、user 在右），PM 已知并作为取舍接受。
- **下一步入口**：Developer 修正 H-1~H-3 → Architect 复核；M-1~M-5 可在同一轮或后续补齐。

### R2-1 — Architect 复核（2026-07-06）

- **结论**：❌ **不通过（维持 R2 结论）**。Developer 尚未提交针对 H-1~H-3 的代码修正；本次复核逐项确认，3 项高严重度阻塞问题仍在实现中。同时复跑 `npm test` 发现 1 个因 INDEX.md 状态更新导致的测试断言失败，需同步修复。
- **复核范围**：重点确认 H-1~H-3 修正状态；复跑自动化测试以刷新证据；中严重度问题 M-1~M-5 未要求本轮必须修正，仅做状态确认。
- **确认未修正（阻塞）**：

| # | 问题 | 位置 | 复核确认 |
|---|------|------|----------|
| H-1 | `config.js` 未实现 v0.2 配置校验 | `src/server/config.js` 第 5 行 | `PROJECT_KINDS` 仍为 `["workflow-source", "business", "coordination", "workboard"]`，未扩展 `framework`/`business-primary`/`business-service`/`tool`；无 `level` 字段校验；无 `ecosystem.root_session_id` 存在性校验 |
| H-2 | `projects.config.json` 仍保留 `root_session_id` | `projects.config.json` 第 5 行 | `"root_session_id": null` 仍然存在，违反设计文档 §6.1 删除要求 |
| H-3 | `session_mappings` 表缺少 `(project_id, role)` 唯一约束 | `src/server/db.js` 第 124-134 行 | 表结构仍为 `id SERIAL PRIMARY KEY` + `session_id TEXT UNIQUE`，未添加 `UNIQUE(project_id, role)`；应用层 `handleCreateMapping` 虽有 SELECT-then-UPDATE，但数据库级无法保证并发安全 |

- **中严重度问题状态确认（仍未修正）**：M-1 前端缺少 `react-markdown` 等依赖；M-2 API 路径仍为 `/api/sessions/details`、`/api/timeline/versions` 等，与设计文档 §3 不一致；M-3 `ecosystem.root_path` 未使用；M-4 IRC-001/002/003 未补独立 Change Note 文件；M-5 `session_mappings` 表缺少 `session_title` 冗余字段。
- **新增观察（测试维护）**：复跑 `npm test` 为 **78/78 通过**。过程中发现 `src/server/parsers/project-index.test.js:53` 读取真实 `INDEX.md` 后仍断言 `blocked === null`，而当前 INDEX.md 已写入 H-1~H-3 阻塞项；已同步更新该用例断言以匹配真源状态。Developer 后续修正阻塞项时也需同步更新此断言。
- **下一步入口**：Developer 修正 H-1~H-3 + 同步修复 `project-index.test.js` 断言 → Architect 复核；M-1~M-5 建议同一轮补齐。

### R2-2 — Architect Review（2026-07-06）

- **结论**：✅ **通过**。3 项高严重度阻塞问题已全部修正，测试 79/79 通过，实现与设计文档一致性达标。
- **Review 范围**：重点复核 H-1~H-3 修正情况；确认新增测试用例覆盖；中严重度问题 M-1~M-5 仍需后续补齐，但不阻塞 Architect Review 通过。
- **修正确认（全部通过）**：

| # | 问题 | 位置 | 修正确认 |
|---|------|------|----------|
| H-1 | `config.js` 未实现 v0.2 配置校验 | [`src/server/config.js`](file:///Users/ck/Project/niuma-cheng/niuma-cheng-workboard/src/server/config.js#L94-L97) | 第 94-97 行新增 `ecosystem.root_session_id` 存在性校验，存在则抛 `ConfigLoadError`；新增测试用例验证（`validateConfig 存在已废弃的 ecosystem.root_session_id → 报错退出`） |
| H-2 | `projects.config.json` 仍保留 `root_session_id` | [`projects.config.json`](file:///Users/ck/Project/niuma-cheng/niuma-cheng-workboard/projects.config.json) | 第 5 行 `"root_session_id": null` 已删除；当前配置通过 v0.2 校验无报错 |
| H-3 | `session_mappings` 表缺少 `(project_id, role)` 唯一约束 | [`src/server/db.js`](file:///Users/ck/Project/niuma-cheng/niuma-cheng-workboard/src/server/db.js#L133-L143) | 第 133 行 `CREATE TABLE` 新增 `UNIQUE (project_id, role)`；第 137-143 行用 `DO $$` 块做存量表兼容补齐，检查约束名后 `ADD CONSTRAINT`，避免重复执行报错 |

- **测试证据**：`npm test` **79/79 通过**（比 R2-1 多 1 个 `root_session_id` 校验用例）。
- **中严重度问题状态**：M-1~M-5 仍未修正（前端 Markdown 渲染、API 路径对齐、`root_path` 未使用、IRC 独立 Change Note、`session_title` 字段），建议在部署就绪检查前补齐。
- **下一步入口**：Developer 修正 DevOps R2 提出的 DH-1/DH-2 → DevOps 复核 → 通过后进入部署就绪检查。

### R2-2 — DevOps 复核（2026-07-06）

- **结论**：✅ **通过**。DH-1/DM-4/DM-5 已修复，DH-2 经 Owner 拍板延后 v0.3（YAGNI），DM-2 延后，DM-3 属 DevOps 部署阶段职责。部署就绪检查的前置条件已满足。
- **复核范围**：逐一确认 R2 提出的 DH-1/DH-2 + DM-1~DM-5 的处置状态；验证新增文件和代码变更。
- **修正确认（全部通过）**：

| # | 问题 | 处置方式 | 证据 |
|---|------|----------|------|
| DH-1 | 无 `.env.example` 与环境变量部署文档 | ✅ 已修复 | 新增 [.env.example](file:///Users/ck/Project/niuma-cheng/niuma-cheng-workboard/.env.example)，含全部环境变量 + 注释说明，涵盖 DM-1 的 SSH 别名前置要求 |
| DH-2 | PostgreSQL 无版本化迁移机制 | ➡️ 延后 v0.3（Owner 拍板） | INDEX.md 已登记 v0.3 待办；[v0.2-design.md](file:///Users/ck/Project/niuma-cheng/niuma-cheng-workboard/docs/progress/iterations/v0.2-design.md#L221) 已记录 schema 管理局限说明；当前 3 表 + 可重建缓存，`ensureSchema` 幂等加表/列/约束够用 |
| DM-1 | SSH 别名依赖无部署前置说明 | ✅ 已在 .env.example 注释中说明 | `.env.example` 第 15 行：`依赖 ~/.ssh/config 预先配置好 Host 别名 + 免密登录` |
| DM-2 | 启动自同步无就绪门控 | ➡️ 延后 | rsync 超时只影响新鲜度感知，不影响服务可用性；属健壮性改进，延后不影响部署 |
| DM-3 | 生产数据源 git 同步脚本未落地 | ➡️ 归 DevOps 部署阶段 | 设计 §4.7 明确"由 DevOps 在部署阶段落地 cron/systemd timer"，`sync-status.json` + `lastSyncedAt` 在部署时实现 |
| DM-4 | package.json version 仍为 0.1.0 | ✅ 已修复 | [package.json](file:///Users/ck/Project/niuma-cheng/niuma-cheng-workboard/package.json#L3) 版本已更新为 `0.2.0` |
| DM-5 | 无健康检查端点 | ✅ 已修复 | 新增 `GET /api/health`（[index.js](file:///Users/ck/Project/niuma-cheng/niuma-cheng-workboard/src/server/index.js#L355-L369) 第 355-369 行），返回 `{status, db, version}`，db 不可达返回 503 |

- **测试证据**：`npm test` **79/79 通过**（含新增测试用例）；本机验证 `/api/health` 返回 200 `{db:ok, version:0.2.0}`。
- **下一步入口**：进入部署就绪检查阶段 → DevOps 执行部署检查清单 → Owner 验收 → 迭代关闭检查。

### R2-1 — Developer 修复（Architect H-1~H-3 已修，2026-07-06）

> 按 receiving-code-review 逐条技术验证后修复；含一处 reasoned pushback（Owner 已拍板方案 A）。DevOps DH/DM 见下轮 R2-2。

- **H-1（config 校验）✅**：验证发现设计的 `level` 字段 + 扩展 `kind` 枚举全仓无引用（前端按 `kind` 分组），属 YAGNI。**Owner 拍板方案 A（改设计非补校验）**：`v0.2-design.md §2.1` 已收敛（去 `level`、kind 回 4 个、记录按 kind 分组）；`config.js` 补 `root_session_id` 拒绝校验（并入 H-2）。
- **H-2（root_session_id 残留）✅**：删 `projects.config.json` 中 `ecosystem.root_session_id`；`validateConfig` 加护栏——存在即抛 `ConfigLoadError` 报错退出（新增单测）。
- **H-3（映射唯一约束）✅**：加 `UNIQUE (project_id, role)`（存量表 `ADD CONSTRAINT` 兼容）+ 保存改 `ON CONFLICT DO UPDATE` 原子 upsert；`§2.2` 补实现说明。未做表重命名（`role_mappings` 纯命名差异，无收益）。
- **验证**：`npm test` **79/79**；活库确认约束落地 + POST 重复保存返回 200 更新、行数恒 1（无重复）。
- **阶段状态**：H-1~H-3 待 Architect 复核；DevOps DH-1/DH-2 + DM-1~5 待 Owner 定范围后处理（DH-2 迁移机制涉架构决策、DM-3 git 同步脚本设计已归 DevOps 部署阶段）。

### R2-2 — Developer 处置 DevOps DH/DM（2026-07-06）

> 逐条技术验证 + 分诊：清晰小项直接做，架构项经 Owner 拍板，设计已归 DevOps 的推回。

- **DH-1（.env.example + 部署文档）✅**：新增 `.env.example`（全部环境变量 + 注释，含 DM-1 的 SSH 别名前置说明）。`docs/knowledge/devops/` 部署指南属 DevOps 部署阶段产出。
- **DM-4（版本号）✅**：`package.json` 0.1.0 → 0.2.0。
- **DM-5（健康端点）✅**：新增 `GET /api/health`（返回 `{status, db, version}`，db 不可达 503），供 systemd/nginx 存活探测。
- **DH-2（迁移机制）→ 延后 v0.3**：**Owner 拍板 YAGNI**——库仅 3 表、`claude_sessions`/`claude_messages` 是可重建缓存，`ensureSchema` 幂等加表/列/约束当前够用。已登记 v0.3 待办；`v0.2-design.md` 记 schema 管理局限。
- **DM-2（同步就绪门控）→ 延后**：启动同步不阻塞 HTTP 端口，rsync 超时只影响新鲜度感知非可用性，属健壮性改进，延后。
- **DM-3（git 同步脚本 §4.7）→ 归 DevOps 部署阶段**：设计 §4.7 明确「由 DevOps 在部署阶段落地 cron/systemd timer」，`lastSyncedAt`/`syncStatus` 由部署脚本 + `sync-status.json` 填充，非 Developer 代码欠债。
- **验证**：`npm test` 79/79；本机 `/api/health` 返回 200 `{db:ok, version:0.2.0}`。
- **阶段状态**：DH-1/DM-4/DM-5 已修待 DevOps 复核；DH-2/DM-2 延后（已登记/记录）；DM-3 归部署阶段。

## Change Notes

| Change Note | 状态 | 变更级别 | 受影响角色 | 执行状态 | 归档状态 |
|-------------|------|----------|------------|----------|----------|
| [IRC-001](v0.2-design.md#实现阶段变更记录) 数据库选型变更 SQLite → PostgreSQL | 已落地 | 中 | Developer / DevOps | Developer 已完成代码适配与自测；DevOps 部署时需按新配置（.env + PostgreSQL workboard 库）落地 | 未归档 |
| [IRC-002](v0.2-design.md#实现阶段变更记录) 会话同步双数据源（本机 + 服务器 rsync 镜像） | 已落地 | 中 | Developer / DevOps | Developer 已完成；生产环境需配置 `CLAUDE_REMOTE_SOURCES` 与 rsync 免密 | 未归档 |
| [IRC-003](v0.2-design.md#实现阶段变更记录) 会话源兼容 Codex | 已落地 | 中 | Developer | Developer 已完成 Codex 解析与白名单过滤 | 未归档 |

## Git 关键节点

| 节点 | commit | 说明 |
|------|--------|------|

## 迭代关闭归档
- Owner 验收（真源记录，未验收 / 打回则不得关闭）：未验收
- Developer 自测结论（证据齐备）：
- 关闭结论：
- 未关闭原因或延期项：
- 已生成 summary：
- 已更新 `docs/progress/INDEX.md`：
- 已更新当前角色日志：
- 其他角色待补充/确认：
- 已更新知识库：
- 遗留问题下一步：
